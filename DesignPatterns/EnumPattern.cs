namespace DesignPatterns
{
    public enum EnumPattern
    {
        Singleton = 1,
        FactoryMethod,
        AbstractFactory,
        Builder,
        Prototype,
        Adapter,
        Bridge,
        Composite,
        Decorator
    }
}
